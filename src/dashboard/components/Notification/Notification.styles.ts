import { css } from '@emotion/react'
import styled from 'shared/styles/styled'
import Card from 'dashboard/components/Card'

export const NotificationWrapper = styled(Card)(({ theme }) => css`
  width: 350px;
`);

